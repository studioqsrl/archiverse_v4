apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: archiverse
  namespace: archiverse
spec:
  tls:
  - hosts:
    - archiverse.studioq.biz
    secretName: archiverse-tls
  rules:
  - host: archiverse.studioq.biz
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: archiverse
            port:
              number: 80
