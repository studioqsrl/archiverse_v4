apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: archiverse
spec:
  template:
    spec:
      containers:
      - name: app-pool
        image: archiverseacr.azurecr.io/frontend:cb8r  # Using specific Run.ID tag for immutable reference
